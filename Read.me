Scanner is used to scan a network
Scapy is a simple program where an user can view a summary of an TCP layer
To run the code:
sudo python3 scanner.py [interface] [ip_range]
interface = eth0
ip_range = 192.168.0.1/24 (change to the wanted IP)